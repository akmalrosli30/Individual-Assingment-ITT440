   //Exit looping
                        if (compare_strings(buffer, "exit")==0)
                                {
                                exit = 1;
                                }
                        }
                }

                return 0;
        }
